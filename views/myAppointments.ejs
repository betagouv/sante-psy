<div class="rf-col-lg-4 rf-col-xs-10 rf-mr-9w">
  <h2>
    Mes séances
  </h2>
  <% if (errors.length) { %>
    <% errors.forEach(function(error) { %>
      <%- include('partials/notificationError', {error: error}) -%>
    <% }) %>
  <% } %>
  <% if (infos.length) { %>
    <% infos.forEach(function(info) { %>
      <%- include('partials/notificationInfo', {info: info}) -%>
    <% }) %>
  <% } %>
  <div class="rf-my-5w rf-pb-2w">
    <ul>
    <% appointments.forEach(function(appointment) { %>
      <li><%= format.formatFrenchDate(appointment.appointmentDate) %> avec <%= appointment.firstNames %> <%= appointment.lastName %></li>
    <% }) %>
  </ul>
  </div>
  <div>
    <a href="/nouvelle-seance" class="rf-btn rf-fi-add-line rf-btn--icon-left">Nouvelle séance</a>
  </div>
</div>


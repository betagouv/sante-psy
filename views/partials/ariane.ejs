<nav class="fr-breadcrumb" aria-label="vous êtes ici :" role=”navigation”>
    <button class="fr-breadcrumb__button" hidden>Voir le fil d’Ariane</button>
    <ol class="fr-breadcrumb__list">
        <!-- Display the parent pages, except current page -->
        <% for (var i = 0; i < items.length -1; i++) { %>
            <li class="fr-breadcrumb__link">
                <% if (items[i].url) { %>
                    <a href="<%= items[i].url %>"><%= items[i].label %></a>
                <% } else { %>
                    <%= items[i].label %>
                <% } %>
            </li>
        <% } %>
        <!-- Display the current page-->
        <li class="fr-breadcrumb__link"
            aria-current="page">
            <%= items[items.length - 1].label %>
        </li>
    </ol>
</nav>
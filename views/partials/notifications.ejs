<% errors.forEach((error, index) => { %>
      <%- include('notificationError', {error, index}) -%>
<% }) %>
<% infos.forEach((info, index) => { %>
      <%- include('notificationInfo', {info, index}) -%>
<% }) %>
